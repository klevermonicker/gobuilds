format_version: 3
environments:
  Dev:
    environment_variables:
      DEPLOYMENT: dev
  pipelines:
    buildtesting:
      group: infrastructure
      #label_template: "${gitMaterial[:8]}"
      #lock_behavior: none
      materials:
        gitMaterial:
          git: "git@communityfunded/cf-static-build.git"
          branch: "develop"
          auto_update: false
          shallow_clone: true
      stages:
        - build: #name of stage
          jobs:
            build: #name of job
              tasks:
                -exec: #Task type
                  command: make